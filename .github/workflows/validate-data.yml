name: Validate database PubMed links

# Roda a cada push que altere o dataset (data.js) e valida que TODOS os
# pubmed_url apontam para o PMID correto do ensaio (via NCT[si] + título
# do ClinicalTrials.gov). Falha o check se houver PMID fabricado/errado —
# este é o guard permanente contra a contaminação de pubmed_url.
#
# Também pode ser disparado manualmente (workflow_dispatch).

on:
  push:
    branches: [main]
    paths:
      - 'assets/js/data.js'
      - 'scripts/validate_pubmed.mjs'
      - 'scripts/validate_nct.mjs'
  pull_request:
    paths:
      - 'assets/js/data.js'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: validate-data-${{ github.ref }}
  cancel-in-progress: true

jobs:
  validate:
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Validate pubmed_url integrity
        run: node scripts/validate_pubmed.mjs --json pubmed-report.json

      - name: Validate ClinicalTrials.gov (NCT) integrity
        if: always()
        run: node scripts/validate_nct.mjs --json nct-report.json

      - name: Upload reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: data-validation-reports
          path: |
            pubmed-report.json
            nct-report.json
          if-no-files-found: ignore
